# Application dependencies

require:
    - play -> crud
    - net.coobird.thumbnailator.Thumbnails -> Thumbnailator 0.3.10-all

repositories:
    - provided:
        type:       local
        artifact:   "${application.path}/jar/[module]-[revision].jar"
        contains:
            - net.coobird.thumbnailator.Thumbnails -> *